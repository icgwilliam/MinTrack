name: Run MinTrack Telegram Bot

# Ejecuta el bot de Telegram (con menú, suscripciones y centinela) en
# long-polling dentro de un runner de GitHub Actions. Reinicia según el cron.
#
# El bot incluye notificaciones proactivas (centinela): un job revisa las
# suscripciones activas contra la ANM cada MINTRACK_CENTINELA_MIN minutos y
# notifica liberaciones de área, cambios de estado/etapa y vencimientos.
#
# IMPORTANTE - limitaciones del plan gratuito de GitHub Actions:
#   * Cada job dura máximo ~6h; GitHub lo cancela al superar ese límite.
#   * 2.000 minutos/mes de cuota gratuita en cuentas personales.
#   * GitHub puede pausar workflows que llevan >60 días sin commits en el repo.
# Por eso el cron reinicia el bot periódicamente. Esto significa que las
# notificaciones del centinela PUEDEN RETRASARSE horas (ventanas sin servicio).
# Para un servicio realmente 24/7 y estable (notificaciones en tiempo real),
# se recomienda un runner self-hosted o un PaaS/VPS. Ver README.md.

on:
  push:
    branches: [main]
    paths:
      - "mintrack/**"
      - "existing_scripts/**"
      - "requirements.txt"
      - "pyproject.toml"
      - ".github/workflows/run-bot.yml"
  schedule:
    # Cada 5 horas: reemplaza la instancia antes del límite de 6 horas.
    - cron: "0 */5 * * *"
  workflow_dispatch:
    # Permite lanzarlo manualmente desde la pestaña Actions.

# Evita ejecuciones concurrentes y reemplaza la instancia anterior cuando un
# push o lanzamiento manual despliega código nuevo. El bot usa getUpdates con
# offset, así que los reinicios no duplican mensajes.
concurrency:
  group: mintrack-bot
  cancel-in-progress: true

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 350  # algo menos de 6h para que GitHub no lo mate a la fuerza
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      LOG_LEVEL: INFO
      # Persistencia de la base de datos y documentos dentro del workspace.
      MINTRACK_DB_PATH: ${{ github.workspace }}/data/mintrack.db
      MINTRACK_DOC_DIR: ${{ github.workspace }}/data/docs
      # Frecuencia del centinela (revisión de suscripciones), en minutos.
      MINTRACK_CENTINELA_MIN: "30"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Restore data cache (DB + documentos)
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/data
          # La key cambia con cada commit; para persistir entre reinicios del
          # mismo commit usamos restore-keys (mantiene la última cache válida).
          key: mintrack-data-${{ github.sha }}
          restore-keys: |
            mintrack-data-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Check token is set
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
            echo "::error::Falta el secret TELEGRAM_BOT_TOKEN. Configúralo en Settings > Secrets and variables > Actions."
            exit 1
          fi

      - name: Run bot
        run: |
          python -m mintrack.bot
